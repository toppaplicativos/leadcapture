{
  "name": "typeui_list_design_systems",
  "description": "List TypeUI registry design skills. Installing one through MCP creates or updates a workspace project before returning files.",
  "inputSchema": {
    "additionalProperties": false,
    "properties": {
      "searchQuery": {
        "anyOf": [
          {
            "type": "string"
          },
          {
            "type": "null"
          }
        ],
        "default": null,
        "description": "Optional search by design skill name or slug."
      },
      "limit": {
        "anyOf": [
          {
            "type": "integer"
          },
          {
            "type": "null"
          }
        ],
        "default": null,
        "description": "Maximum number of design skills to return. Defaults to 24."
      }
    },
    "type": "object"
  }
}